import { UploadedFile } from 'express-fileupload';
import { ErrorMessages, ErrorMessagesOptional, Options, OptionsOptional } from './types';
export declare class ExpressFileuploadValidator {
    private options;
    private errorMessages;
    constructor(options?: OptionsOptional, errorMessages?: ErrorMessagesOptional);
    getOptions(): Options;
    setOptions(options: OptionsOptional): void;
    getErrorMessages(): ErrorMessages;
    setErrorMessages(errorMessages: ErrorMessagesOptional): void;
    validate(uploadedFiles: UploadedFile | UploadedFile[]): void;
    private validateCount;
    private validateFiles;
    private validateFile;
    private getValidators;
}
//# sourceMappingURL=express-fileupload-validator.d.ts.map